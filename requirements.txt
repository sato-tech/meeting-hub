# meeting-hub — HF Spaces (CPU Basic) 用 slim 依存
#
# 重い ML（faster-whisper / pyannote / torch / librosa / noisereduce / sounddevice / scipy）は
# Modal Labs (scripts/modal_deploy.py) 側で pip_install されるため、HF Spaces では不要。
# ローカルで transcribe.runtime=local / diarize.runtime=local として動かすには、
# requirements-ml.txt も併せてインストールする:
#
#   pip install -r requirements.txt -r requirements-ml.txt

# ─── LLM（llm_cleanup / minutes_extract provider＝HF Spaces 内で実行） ───
anthropic>=0.40.0

# ─── スキーマ／設定／テンプレ ───
pydantic>=2.5
# Python 3.9 では Pydantic が str | None 等を評価するのに必要（3.10+ では任意）
eval_type_backport>=0.2.0
PyYAML>=6.0
jinja2>=3.1
python-dotenv>=1.0.0

# ─── Google Drive（サービスアカウント、§12-5） ───
google-api-python-client>=2.120.0
google-auth>=2.28.0

# ─── 外部連携（Phase 2） ───
notion-client>=2.2
slack-sdk>=3.27

# ─── Web UI / Runtime（Phase 3） ───
# protobuf 4.21+ は古い生成コードと非互換で Streamlit 起動時に
# 「Descriptors cannot be created directly」となるため上限を固定する
protobuf>=3.20,<4.21
# Streamlit が起動時に Plotly テーマを読み込む。plotly<5 だと template.data.Icicle がなく AttributeError になる
plotly>=5.0
streamlit>=1.35
modal>=0.64

# ─── UX／ユーティリティ ───
tqdm>=4.66.0
